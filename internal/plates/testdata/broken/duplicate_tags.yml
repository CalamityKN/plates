name: duplicate_tags
category: scanning
description: Plate with duplicate tags
tags:
  - nmap
  - nmap
options:
  target:
    description: Target IP address or hostname
    required: true
template: |
  nmap {{target}}
